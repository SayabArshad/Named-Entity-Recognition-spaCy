# Named-Entity-Recognition-spaCy
A Python project that performs Named Entity Recognition (NER) on text using spaCy's pre-trained model. Extracts entities like organizations, locations, monetary values, and saves the results to a CSV file.
